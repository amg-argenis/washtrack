spring.application.name=washtrack-api

base.path=/washtrack/api/v1

# PROPIEDADES PARA BASE DE DATOS MYSQL
spring.datasource.url=jdbc:mysql://localhost:3306/bd_productos?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# PROPIEDADES PARA FILE SYSTEM

# PROPIEDADES OAUTH

# PROPIEDADES JWT